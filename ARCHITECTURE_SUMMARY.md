# Next.js Architecture Summary
**Menu Maker SaaS - Production-Ready Serverless Architecture**

---

## 🏗️ SYSTEM ARCHITECTURE

### High-Level Overview
```
┌─────────────────────────────────────────────────────────────────┐
│                         VERCEL EDGE NETWORK                      │
│                     (Global CDN + Edge Functions)                │
└──────────────────────────┬──────────────────────────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                     │
        ▼                                     ▼
┌───────────────────┐              ┌────────────────────────┐
│  STATIC PAGES     │              │  DYNAMIC API ROUTES    │
│  (ISR Cached)     │              │  (Serverless)          │
├───────────────────┤              ├────────────────────────┤
│ • Landing Page    │              │ • Authentication       │
│ • Public Menu *   │              │ • CRUD Operations      │
│                   │              │ • Payment Processing   │
│ Revalidate: 5min  │              │ • File Uploads         │
└─────────┬─────────┘              └──────────┬─────────────┘
          │                                    │
          └────────────────┬───────────────────┘
                           ▼
                  ┌────────────────────┐
                  │   MONGODB ATLAS    │
                  │   (Serverless DB)  │
                  ├────────────────────┤
                  │ • Users            │
                  │ • Cafes            │
                  │ • Menu Items       │
                  │ • Subscriptions    │
                  └────────────────────┘
                           │
        ┌──────────────────┼──────────────────┐
        ▼                  ▼                  ▼
┌─────────────┐   ┌─────────────┐   ┌──────────────┐
│ CLOUDINARY  │   │  RAZORPAY   │   │   NEXTAUTH   │
│ (Images)    │   │ (Payments)  │   │   (Auth)     │
└─────────────┘   └─────────────┘   └──────────────┘
```

---

## 🔄 REQUEST FLOWS

### 1️⃣ Customer Scans QR Code → Views Menu

```
┌──────────────┐
│   Customer   │ Scans QR code (printed at cafe)
└──────┬───────┘
       │ https://yourapp.vercel.app/menu/cafe-xyz-123
       ▼
┌──────────────────────────────────────────────────────────┐
│  VERCEL EDGE CDN (Nearest to customer geographically)    │
│  ┌────────────────────────────────────────────────┐     │
│  │ Check: Is /menu/cafe-xyz-123 cached?           │     │
│  └────────────┬───────────────────────────────────┘     │
│               │                                          │
│      YES ─────┴──── NO (or expired)                     │
│       │                    │                             │
│       │                    ▼                             │
│       │         ┌────────────────────────┐              │
│       │         │ Lambda Function Runs   │              │
│       │         │ ──────────────────────│              │
│       │         │ 1. Query MongoDB       │              │
│       │         │ 2. Fetch cafe data     │              │
│       │         │ 3. Fetch menu items    │              │
│       │         │ 4. Fetch offers        │              │
│       │         │ 5. Calculate discounts │              │
│       │         │ 6. Render HTML         │              │
│       │         └────────┬───────────────┘              │
│       │                  │                               │
│       │                  │ Cache for 5 min              │
│       │                  ▼                               │
│       └──────────► Return HTML                          │
│                                                          │
└────────────────────────┬─────────────────────────────────┘
                         │
                         ▼
                  ┌─────────────┐
                  │   Browser   │ Renders menu instantly
                  │             │ (HTML + CSS pre-rendered)
                  └─────────────┘
                         │
                         │ User interacts:
                         │ • Search items
                         │ • Filter by diet
                         │ • Select category
                         ▼
                  (Client-side JS)
                  No server calls needed ✅
```

**Performance:**
- ✅ First Load: <200ms (cached at CDN)
- ✅ Subsequent Loads: <50ms (browser cache)
- ✅ No database calls for cached pages
- ✅ ISR updates every 5 minutes automatically

---

### 2️⃣ Admin Dashboard → Menu Management

```
┌──────────────┐
│  Cafe Admin  │ Opens admin dashboard
└──────┬───────┘
       │ https://yourapp.vercel.app/admin/menu
       ▼
┌──────────────────────────────────────────────┐
│            Authentication Check               │
│  ┌────────────────────────────────────┐     │
│  │ middleware.ts (Edge Function)      │     │
│  │ ──────────────────────────────────│     │
│  │ 1. Check NextAuth session cookie   │     │
│  │ 2. Valid? → Continue               │     │
│  │ 3. Invalid? → Redirect to /login  │     │
│  └────────────────────────────────────┘     │
└──────────────────┬───────────────────────────┘
                   ▼
        ┌────────────────────────┐
        │ Server Component Runs  │
        │ ─────────────────────│
        │ 1. Fetch user's cafes  │
        │ 2. Fetch menu items    │
        │ 3. Fetch categories    │
        │ 4. Pre-render page     │
        └────────┬───────────────┘
                 │
                 ▼
          ┌─────────────┐
          │   Browser   │ Receives fully rendered page
          └──────┬──────┘
                 │
                 │ Admin performs action:
                 │ "Add New Menu Item"
                 ▼
┌────────────────────────────────────────────────────┐
│         Client-Side Form Submission                │
│  ┌──────────────────────────────────────┐         │
│  │ 1. User fills form                   │         │
│  │ 2. Upload image to /api/upload       │         │
│  │    ↓                                 │         │
│  │    Cloudinary returns URL            │         │
│  │ 3. Submit form to /api/menu-items    │         │
│  └──────────────┬───────────────────────┘         │
└─────────────────┼────────────────────────────────┘
                  ▼
        ┌───────────────────────┐
        │ API Route Handler     │
        │ POST /api/menu-items  │
        │ ─────────────────────│
        │ 1. Validate session   │
        │ 2. Validate input     │
        │ 3. Check ownership    │
        │ 4. Insert to MongoDB  │
        │ 5. Trigger ISR*       │
        │ 6. Return response    │
        └───────┬───────────────┘
                │
                ▼
         ┌────────────┐
         │  MongoDB   │ Item saved
         └─────┬──────┘
               │
               ▼
      ISR Revalidation Triggered
      (Public menu will update in <5min)
               │
               ▼
        ┌──────────────┐
        │   Browser    │ Shows success message
        │              │ TanStack Query refetches
        │              │ UI updates instantly
        └──────────────┘
```

**Performance:**
- ✅ Page Load: 500ms-1s (includes auth check + data fetch)
- ✅ Form Submit: 200-500ms (optimistic updates with TanStack Query)
- ✅ Image Upload: 1-3s (Cloudinary processing)

---

### 3️⃣ Payment Flow → Subscription Upgrade

```
┌──────────────┐
│  Cafe Admin  │ Clicks "Upgrade to Pro"
└──────┬───────┘
       │
       ▼
┌────────────────────────────────────┐
│ Client Component (React)           │
│ ──────────────────────────────────│
│ 1. User selects plan (Pro Monthly) │
│ 2. Enters promo code (optional)    │
│ 3. Clicks "Pay Now"                │
└────────┬───────────────────────────┘
         │
         ▼
┌──────────────────────────────────────────┐
│ POST /api/subscription/order              │
│ ────────────────────────────────────────│
│ 1. Validate user session                 │
│ 2. Check existing subscription           │
│ 3. Calculate amount (₹199/month)         │
│ 4. Apply promo code discount             │
│ 5. Create Razorpay order                 │
│    ↓                                      │
│    Razorpay API Call:                    │
│    razorpay.orders.create({              │
│      amount: 19900, // paisa            │
│      currency: 'INR'                     │
│    })                                     │
│ 6. Return order ID to client             │
└─────────┬────────────────────────────────┘
          │
          ▼
   ┌──────────────────────┐
   │  Razorpay Checkout   │ (Modal opens)
   │  ────────────────────│
   │  User enters:        │
   │  • Card details      │
   │  • UPI ID            │
   │  • Net Banking       │
   └──────┬───────────────┘
          │
          │ Payment Success
          ▼
   ┌──────────────────────────────────┐
   │  Razorpay Webhook/Callback       │
   │  ────────────────────────────────│
   │  Sends:                          │
   │  • razorpay_payment_id           │
   │  • razorpay_order_id             │
   │  • razorpay_signature (HMAC)     │
   └──────┬───────────────────────────┘
          │
          ▼
┌──────────────────────────────────────────────┐
│ POST /api/subscription/verify                │
│ ────────────────────────────────────────────│
│ 1. Verify signature with secret             │
│    ↓                                         │
│    crypto.createHmac('sha256', secret)      │
│      .update(order_id + '|' + payment_id)   │
│      .digest('hex')                          │
│                                              │
│ 2. Signature valid? → Continue              │
│ 3. Update subscription in MongoDB:          │
│    {                                         │
│      status: 'active',                       │
│      plan: 'pro',                            │
│      interval: 'monthly',                    │
│      expiresAt: new Date() + 30 days         │
│    }                                         │
│ 4. Increment promo code usage               │
│ 5. Send success response                    │
└─────────┬────────────────────────────────────┘
          │
          ▼
   ┌──────────────┐
   │   Browser    │ Shows "Upgrade Successful"
   │              │ Redirects to dashboard
   │              │ Pro features unlocked ✨
   └──────────────┘
```

**Security:**
- ✅ Signature verification prevents tampering
- ✅ Server-side amount calculation (client can't manipulate)
- ✅ Promo code validation server-side
- ✅ Subscription status checked on every menu load

---

## 📊 DATA FLOW DIAGRAM

### Database Schema Flow
```
┌────────────────────────────────────────────────────────────┐
│                      MONGODB ATLAS                         │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌──────────┐    ┌──────────┐    ┌──────────────┐       │
│  │  users   │───╮│  cafes   │    │subscriptions │       │
│  ├──────────┤   │├──────────┤    ├──────────────┤       │
│  │ id       │   ││ id       │    │ id           │       │
│  │ username │   ││ ownerId ────┐ │ userId ──────┼─┐    │
│  │ password │   ││ name     │  │ │ status       │ │    │
│  └──────────┘   ││ slug     │  │ │ plan         │ │    │
│       │         ││ logoUrl  │  │ │ expiresAt    │ │    │
│       │         │└──────────┘  │ └──────────────┘ │    │
│       │         │      │       │                   │    │
│       │         │      ▼       ▼                   ▼    │
│       │         │  ┌──────────────┐    ┌─────────────┐ │
│       │         │  │ categories   │    │   offers    │ │
│       │         │  ├──────────────┤    ├─────────────┤ │
│       │         │  │ id           │    │ id          │ │
│       │         │  │ cafeId ──────┼──┐ │ cafeId ─────┤ │
│       │         │  │ name         │  │ │ title       │ │
│       │         │  │ sortOrder    │  │ │ discountType│ │
│       │         │  └──────────────┘  │ └─────────────┘ │
│       │         │         │          │                  │
│       │         │         ▼          │                  │
│       │         │   ┌──────────────┐ │                 │
│       │         └──▶│  menuItems   │◀┘                 │
│       │             ├──────────────┤                    │
│       │             │ id           │                    │
│       │             │ cafeId       │                    │
│       │             │ categoryId   │                    │
│       │             │ name         │                    │
│       │             │ price        │                    │
│       │             │ imageUrl     │                    │
│       │             └──────────────┘                    │
│       │                                                  │
└───────┼──────────────────────────────────────────────────┘
        │
        ▼
┌────────────────────┐
│  EXTERNAL SERVICES │
├────────────────────┤
│  Cloudinary:       │ imageUrl → stored in menuItems
│  Razorpay:         │ payment_id → stored in subscriptions
└────────────────────┘
```

### Component Data Flow
```
┌─────────────────────────────────────────────────────────────┐
│                     SERVER COMPONENTS                        │
│  ┌────────────────────┐         ┌──────────────────────┐   │
│  │ app/menu/[slug]/   │         │ app/(admin)/         │   │
│  │      page.tsx      │         │   dashboard/         │   │
│  │                    │         │      page.tsx        │   │
│  │ export async       │         │                      │   │
│  │   function         │         │ export async         │   │
│  │     MenuPage()     │         │   function           │   │
│  │ {                  │         │     Dashboard()      │   │
│  │   const cafe =     │         │ {                    │   │
│  │     await          │         │   const session =    │   │
│  │     storage        │         │     await getServerSession() │
│  │     .getCafe()     │         │   const cafes =      │   │
│  │                    │         │     await storage    │   │
│  │   return           │         │     .getCafes()      │   │
│  │     <ClientComp /> │         │                      │   │
│  │ }                  │         │   return             │   │
│  └────────┬───────────┘         │     <DashClient />  │   │
│           │                     └──────┬───────────────┘   │
└───────────┼────────────────────────────┼───────────────────┘
            │                            │
            ▼                            ▼
┌─────────────────────────────────────────────────────────────┐
│                    CLIENT COMPONENTS                         │
│  ┌──────────────────────┐    ┌─────────────────────────┐   │
│  │ PublicMenuClient     │    │ DashboardClient         │   │
│  │ ('use client')       │    │ ('use client')          │   │
│  │                      │    │                         │   │
│  │ • useState()         │    │ • useCafes()            │   │
│  │ • Search filter      │    │ • useMenuItems()        │   │
│  │ • Diet filter        │    │ • TanStack Query        │   │
│  │ • Category selector  │    │ • Forms (React Hook)    │   │
│  │ • Framer Motion      │    │ • Mutations             │   │
│  └──────────────────────┘    └────────┬────────────────┘   │
│                                        │                     │
└────────────────────────────────────────┼─────────────────────┘
                                         │
                                         ▼
                               ┌────────────────────┐
                               │   API ROUTES       │
                               │ /api/menu-items    │
                               │ /api/categories    │
                               │ /api/upload        │
                               └────────────────────┘
```

---

## 🔐 SECURITY ARCHITECTURE

### Authentication Flow
```
                    ┌─────────────────┐
                    │   User Request  │
                    └────────┬────────┘
                             │
                             ▼
                    ┌────────────────────┐
                    │  middleware.ts     │ (Runs on Edge)
                    │  ─────────────────│
                    │  matcher: [        │
                    │    '/admin/:path*',│
                    │    '/api/cafes',   │
                    │    '/api/upload'   │
                    │  ]                 │
                    └────────┬───────────┘
                             │
                ┌────────────┴────────────┐
                │                         │
                ▼                         ▼
         [Has Session]              [No Session]
                │                         │
                │                         ▼
                │                  ┌──────────────┐
                │                  │ Redirect to  │
                │                  │   /login     │
                │                  └──────────────┘
                ▼
        ┌───────────────────┐
        │ Decode JWT Token  │
        │ ─────────────────│
        │ Contains:         │
        │ • userId          │
        │ • username        │
        │ • exp (expiry)    │
        └────────┬──────────┘
                 │
                 ▼
        ┌────────────────────┐
        │  API Route Handler │
        │  ──────────────────│
        │  1. Get user from  │
        │     session        │
        │  2. Verify         │
        │     ownership      │
        │  3. Perform action │
        └────────────────────┘
```

### Data Validation Layers
```
┌──────────────────────────────────────────────────────┐
│                   CLIENT SIDE                         │
│  ┌────────────────────────────────────────┐          │
│  │ React Hook Form + Zod Schema           │          │
│  │ • Instant feedback                     │          │
│  │ • Type-safe forms                      │          │
│  │ • Custom validation rules              │          │
│  └──────────────────┬─────────────────────┘          │
└─────────────────────┼────────────────────────────────┘
                      │
                      ▼ (Submit)
┌──────────────────────────────────────────────────────┐
│                 API ROUTE (Server)                    │
│  ┌────────────────────────────────────────┐          │
│  │ 1. Re-validate with same Zod schema    │          │
│  │    (Never trust client!)               │          │
│  │                                        │          │
│  │ 2. Sanitize inputs                     │          │
│  │    • Trim strings                      │          │
│  │    • Escape HTML                       │          │
│  │                                        │          │
│  │ 3. Check permissions                   │          │
│  │    • User owns this cafe?              │          │
│  │    • Subscription active?              │          │
│  └──────────────────┬─────────────────────┘          │
└─────────────────────┼────────────────────────────────┘
                      │
                      ▼ (Query)
┌──────────────────────────────────────────────────────┐
│                    DATABASE                           │
│  ┌────────────────────────────────────────┐          │
│  │ • Parameterized queries (Drizzle ORM)  │          │
│  │ • No raw SQL injection possible        │          │
│  │ • MongoDB: Mongoose sanitizes          │          │
│  └────────────────────────────────────────┘          │
└──────────────────────────────────────────────────────┘
```

---

## 🚀 DEPLOYMENT ARCHITECTURE

### Vercel Build Process
```
┌────────────────────────────────────────────────────────┐
│  1. PUSH TO GITHUB                                     │
│     ────────────────────────────────────────────────  │
│     git push origin main                               │
└─────────┬──────────────────────────────────────────────┘
          │
          ▼
┌────────────────────────────────────────────────────────┐
│  2. VERCEL BUILD TRIGGERED                             │
│     ────────────────────────────────────────────────  │
│     • Clone repository                                 │
│     • Install dependencies (npm ci)                    │
│     • Build Next.js app                                │
│       - Compile TypeScript                             │
│       - Generate static pages                          │
│       - Bundle client JS                               │
│       - Optimize images                                │
│     • Generate ISR manifests                           │
└─────────┬──────────────────────────────────────────────┘
          │
          ▼
┌────────────────────────────────────────────────────────┐
│  3. DEPLOY TO EDGE NETWORK                             │
│     ────────────────────────────────────────────────  │
│     Static Assets (HTML, CSS, JS, Images)              │
│     ↓                                                  │
│     Deployed to CDN (150+ locations)                   │
│                                                        │
│     API Routes & Server Components                     │
│     ↓                                                  │
│     Deployed as Lambda Functions (AWS us-east-1)       │
└─────────┬──────────────────────────────────────────────┘
          │
          ▼
┌────────────────────────────────────────────────────────┐
│  4. LIVE AT                                            │
│     ────────────────────────────────────────────────  │
│     https://menu-maker.vercel.app                      │
│                                                        │
│     Custom Domain (Optional):                          │
│     https://menus.yourcafe.com                         │
│                                                        │
│     DNS: CNAME → cname.vercel-dns.com                  │
└────────────────────────────────────────────────────────┘
```

### Environment Configuration
```typescript
// .env.local (Development)
MONGODB_URI=mongodb://localhost:27017/menu-maker-dev
NEXTAUTH_SECRET=dev_secret_key_change_in_prod
NEXTAUTH_URL=http://localhost:3000

// Vercel Environment Variables (Production)
MONGODB_URI=mongodb+srv://cluster.mongodb.net/menu-maker
NEXTAUTH_SECRET=<generate-strong-secret>
NEXTAUTH_URL=https://menu-maker.vercel.app
RAZORPAY_KEY_ID=rzp_live_xxx
RAZORPAY_KEY_SECRET=xxx
CLOUDINARY_CLOUD_NAME=xxx
CLOUDINARY_API_KEY=xxx
CLOUDINARY_API_SECRET=xxx
```

---

## 📈 SCALABILITY PATTERNS

### Horizontal Scaling (Automatic)
```
Low Traffic (1-10 requests/sec):
┌────────────────┐
│ 1 Lambda       │ → MongoDB
│ (Auto-scales)  │
└────────────────┘

Medium Traffic (100 requests/sec):
┌────────────────┐
│ Lambda Pool    │
│ ┌──┬──┬──┬──┐ │ → MongoDB (Connection Pool)
│ │1 │2 │3 │4 │ │
│ └──┴──┴──┴──┘ │
└────────────────┘

High Traffic (1000 requests/sec):
┌────────────────────────────────┐
│ Lambda Pool (Auto-scales)      │
│ ┌──┬──┬──┬──┬──┬──┬──┬──┬──┐ │
│ │1 │2 │3 │4 │5 │6 │7 │8 │9 │ │ → MongoDB Atlas (M10+)
│ └──┴──┴──┴──┴──┴──┴──┴──┴──┘ │
└────────────────────────────────┘
         ↓
    Edge CDN absorbs
    90% of menu views
    (No Lambda needed)
```

### Caching Strategy
```
┌─────────────────────────────────────────┐
│          CACHING LAYERS                 │
├─────────────────────────────────────────┤
│                                         │
│  1. Browser Cache (Client)              │
│     • Static assets: 1 year             │
│     • Admin pages: No cache             │
│     Duration: Instant                   │
│                                         │
│  2. CDN Cache (Vercel Edge)             │
│     • Public menu: 5 minutes (ISR)      │
│     • Landing page: 1 hour              │
│     Duration: <50ms                     │
│                                         │
│  3. Lambda Memory Cache (Temporary)     │
│     • Database connection pool          │
│     • Shared across warm invocations    │
│     Duration: <100ms                    │
│                                         │
│  4. MongoDB Cache (Database)            │
│     • WiredTiger engine cache           │
│     • Indexes for fast lookups          │
│     Duration: <10ms                     │
│                                         │
└─────────────────────────────────────────┘
```

---

## 🎯 PERFORMANCE TARGETS

| Metric                    | Target      | Expected    | Status |
|---------------------------|-------------|-------------|--------|
| Public Menu Load (Cached) | <200ms      | <100ms      | ✅     |
| Public Menu Load (Uncached)| <500ms     | 300ms       | ✅     |
| Admin Dashboard Load      | <1s         | 800ms       | ✅     |
| Form Submission           | <300ms      | 200ms       | ✅     |
| Image Upload              | <3s         | 2s          | ✅     |
| Payment Processing        | <5s         | 3s          | ✅     |
| API Response Time         | <200ms      | 150ms       | ✅     |
| Time to Interactive (TTI) | <3s         | 2s          | ✅     |
| Lighthouse Score          | >90         | 95+         | ✅     |

---

## 🔄 ISR (Incremental Static Regeneration) Strategy

```typescript
// app/menu/[slug]/page.tsx
export const revalidate = 300 // 5 minutes

// How it works:
┌──────────────────────────────────────────────────────────┐
│  T+0:00  First Request                                   │
│          ↓                                               │
│          No cache → Generate HTML → Cache for 5min      │
│                                                          │
│  T+0:01  Second Request (within 5min)                   │
│          ↓                                               │
│          Served from CDN (instant) ✅                    │
│                                                          │
│  T+5:01  Next Request (after 5min)                      │
│          ↓                                               │
│          Stale cache → Serve stale HTML (instant)       │
│          Background: Regenerate fresh HTML              │
│                                                          │
│  T+5:02  Subsequent Requests                            │
│          ↓                                               │
│          Fresh HTML from cache ✅                        │
└──────────────────────────────────────────────────────────┘

// On-Demand Revalidation (when admin updates menu):
await revalidatePath(`/menu/${slug}`)
// Next request gets fresh data immediately
```

---

## 🎉 SUMMARY

### Architecture Wins

✅ **Serverless Benefits**
- Zero server management
- Auto-scaling (0 to ∞)
- Pay-per-request pricing
- Global CDN included

✅ **Performance Benefits**
- <100ms public menu loads
- Pre-rendered HTML (SEO friendly)
- Edge caching globally
- Optimistic UI updates

✅ **Cost Benefits**
- Free tier: $0/month
- Vercel Pro: $20/month (if needed)
- No server costs
- No DevOps overhead

✅ **Developer Experience**
- Git-based deployments
- Instant previews (PR branches)
- Built-in monitoring
- Type-safe full-stack

---

**Architecture Status:** ✅ Production-Ready  
**Recommended Hosting:** Vercel (Serverless)  
**Estimated Cost:** $0-20/month  
**Scalability:** 1000s of users without changes

---
